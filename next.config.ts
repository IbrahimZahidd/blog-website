import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["picsum.photos", "api.dicebear.com"], // Add picsum.photos here
  },
  // Other configurations can go here
};

export default nextConfig;
